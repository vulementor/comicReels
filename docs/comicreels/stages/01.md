# Phân đoạn 1: Chuẩn bị mã nguồn nền FlowKit

**Trạng thái ngày 25/09/2026:** ACCEPTED_WITH_GAPS: anh cho phép sang Đoạn 2; PR #1 Draft, smoke test còn thiếu.

## Đầu ra mong đợi
Tạo fork công khai hoặc nhập đủ lịch sử vào repo private theo lựa chọn đã duyệt; giữ MIT, ghi upstream SHA; backend/dashboard bản gốc chạy thử; đưa tài liệu vào repo; **chưa** viết bộ tách khung.

## Công việc phải xử lý
- [ ] Xác minh fork parent/SHA
- [ ] giữ nguyên LICENSE
- [ ] bổ sung docs qua PR riêng
- [ ] báo hạn chế runtime

## Checkpoint và Definition of Done
- [ ] .
- [ ] Có nhánh/PR/commit, bản ghi file đã đổi, bằng chứng test và rủi ro còn lại trong [CHECKPOINTS](../CHECKPOINTS.md).
- [ ] Anh xác nhận `OK` nghiệm thu **phân đoạn này** trước khi bước sang phân đoạn tiếp.

## Kiểm thử phải có
Kiểm tra parent, SHA Git và changed-files PR, smoke-test khi có môi trường.

## Giới hạn, rủi ro cần giải quyết
PR #1 chưa merge; chưa có build/backend smoke thật.

**Điểm dừng:** Nếu phạm vi quá lớn, tách a/b và nghiệm thu từng tiểu đoạn, không tự thực hiện phân đoạn tiếp theo. Chưa có test nghĩa là `CHƯA CHẠY`, không được tự đánh dấu đạt.
