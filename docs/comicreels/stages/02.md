# Phân đoạn 2: Khởi động và khung giao diện

**Trạng thái ngày 25/09/2026:** IN_PROGRESS: code đã ở PR #2 Draft, chưa có OK nghiệm thu và chưa có build/browser test.

## Đầu ra mong đợi
Mở ứng dụng dễ dùng trên Windows sau thiết lập lần đầu; giao diện tiếng Việt có vùng Dán ảnh; báo đúng trạng thái Google Flow.

## Công việc phải xử lý
- [ ] Tạo React route ComicReels và dashboard cũ
- [ ] preview ảnh qua Ctrl+V/kéo thả/file
- [ ] gọi health và Flow status an toàn

## Checkpoint và Definition of Done
- [ ] .
- [ ] Có nhánh/PR/commit, bản ghi file đã đổi, bằng chứng test và rủi ro còn lại trong [CHECKPOINTS](../CHECKPOINTS.md).
- [ ] Anh xác nhận `OK` nghiệm thu **phân đoạn này** trước khi bước sang phân đoạn tiếp.

## Kiểm thử phải có
Build Vite/TypeScript, mở trang, thử ảnh sai định dạng và tắt backend/extension.

## Giới hạn, rủi ro cần giải quyết
PR #2 chưa merge; chưa xác nhận build hoặc Flow trên trình duyệt.

**Điểm dừng:** Nếu phạm vi quá lớn, tách a/b và nghiệm thu từng tiểu đoạn, không tự thực hiện phân đoạn tiếp theo. Chưa có test nghĩa là `CHƯA CHẠY`, không được tự đánh dấu đạt.
