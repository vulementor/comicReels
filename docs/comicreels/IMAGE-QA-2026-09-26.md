# Đối chiếu ảnh thật trên VULE-PC — 26/09/2026

## Kết quả: IMAGE_QA_FAILED

Người dùng phản ánh ba ảnh trông như chỉ có một. Đối chiếu trực tiếp source/crop và từng portrait qua Remote Desktop Commander xác nhận phản ánh đúng về nội dung. Ba file có hash khác nhau và UI trỏ đến ba panel riêng; đây không phải lỗi dùng chung URL ảnh.

| Khung | Khung gốc | Ảnh 9:16 đang có | Kết luận |
| --- | --- | --- | --- |
| 1 | Bò cam quay phải, há miệng; thỏ và bò có nơ nằm trên giường | Giữ tư thế chính của khung 1 | Giữ nguyên ảnh và trạng thái duyệt hiện có |
| 2 | Bố cục gần hơn; bò ngậm miệng; thân trên/cánh tay thỏ dựng lên | Bò ngậm miệng nhưng bố cục và thỏ nằm có hai tai vẫn giống khung 1 | Thu hồi duyệt; cần ảnh thay thế đúng tư thế khung 2 |
| 3 | Đầu/mõm bò quay rõ sang trái, ngậm miệng | Bò vẫn quay phải, há miệng, gần như khung 1 | Thu hồi duyệt; cần ảnh thay thế đúng hướng khung 3 |

UI smoke trước đó chỉ chứng minh các ô ảnh render và điều hướng hoạt động. SHA, kích thước 9:16 và provider receipt không chứng minh nội dung đúng. Việc từng chấp nhận ảnh khung 2 trong image-history là sai; verdict đã được sửa thành rejected. Hash của portrait khung 3 hiện tại cũng được bổ sung vào danh sách rejected.

## Sửa trạng thái và code

- Trên VULE-PC, sao lưu SQLite và chi tiết dự án trước khi sửa. Kiểm tra đúng hash ảnh và cả ba shot vẫn READY, rồi chỉ bỏ approved_sha256 của khung 2/3, chuyển về AI_IMAGE_READY. Khung 1, bytes của ba ảnh, lời thoại và ba shot được giữ nguyên. Project chuyển về IMAGES_READY.
- Kết quả review trong image-history phải được kiểm tra trước khi duyệt ảnh, dùng lại/tạo storyboard hoặc upload/gửi video, kể cả khi DB còn cờ duyệt cũ.
- Kết quả trả từ provider/cache hoặc khôi phục lịch sử không được thay thế ảnh hiện tại nếu hash đã bị loại. Manifest hỏng phải báo lỗi, không bỏ qua review.
- Guard chỉ thực thi verdict đã ghi nhận; không tự đánh giá được tư thế, biểu cảm hay bố cục của ảnh mới.

## Bằng chứng và việc còn lại

Trong `local-test-data/image-qa-20260926` trên VULE-PC: `before-image-qa.db`, `before-image-qa.json`, `IMAGE-QA-RESULT.json` và log/XML regression. Không đưa ảnh truyện hay DB lên repo public.

Regression mới trước sửa: 6 FAIL, 1 PASS trên Windows, tái hiện việc bỏ qua verdict ở approval/storyboard/video và cache. Sau sửa tại `c76f7a9a02d74f9320ee19cbca2a9b96d6ace898`: **467 unit tests PASS**, 37.92 giây; toàn bộ chạy trong bản sao source riêng trên VULE-PC, không dùng DB đang chạy. Frontend không thay đổi nên không lặp build/lint đã đạt ở gate trước.

Runtime trên VULE-PC đã cập nhật đúng revision này. API thật trả HTTP 409 cùng lý do ảnh đã bị loại khi thử duyệt lại khung 2 và khung 3; state trước/sau hai request giống nhau. Kiểm tra lại hash xác nhận giữ nguyên ba file và ba shot READY, chỉ một ảnh còn được duyệt. Health OK và extension 0.3.2 kết nối lại sau restart. `live-guard-result.json` và `runtime-update.json` lưu bằng chứng.

Chrome hiện có đã reload qua RDC. Màn ba ảnh reference chỉ còn khung 1, báo cần đủ ba ảnh đã duyệt, nút tạo video bị vô hiệu hóa. Ảnh chụp/cây UI lưu tại `ui-qa-reloaded.png`/`.json`. Chưa kiểm tra chất lượng ảnh thay thế vì chưa tạo ảnh mới. Kết quả code PASS không thay đổi kết luận IMAGE_QA_FAILED của bộ ảnh cũ.

Chưa Generate ảnh hoặc video mới. Theo phạm vi bước 4 trong LOCAL-TEST-PLAN, chỉ đối chiếu ảnh sẵn có. Cần anh đồng ý tạo lại riêng khung 2 và 3 từ đúng `p1.png`/`p2.png`, giữ nguyên khung 1; không lấy ảnh đã Generate của khung 1 làm bố cục tham chiếu. Sau đó đối chiếu tư thế/hướng đầu/bố cục từng ảnh trước khi duyệt. Chưa chuyển sang video thật, chưa merge PR #3.
